services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_N8N_WEBHOOK_URL: ${VITE_N8N_WEBHOOK_URL}
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - VITE_N8N_WEBHOOK_URL=${VITE_N8N_WEBHOOK_URL}
